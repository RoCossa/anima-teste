using FluentValidation;
using Web.Api.Models.Request;

namespace Web.Api.Models.Validation
{
    /*public class CadastarAlunoRequestValidator : AbstractValidator<CadastrarAlunoRequest>
    {
        public RegisterUserRequestValidator()
        {
            // cadastar validadores dos requests
            RuleFor(x => x.Nome).Length(2, 30);
            RuleFor(x => x.Login).Length(2, 30);
            RuleFor(x => x.Senha).Length(5, 255);
        }
    }*/
}
